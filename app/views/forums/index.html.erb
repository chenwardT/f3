<div>
  <h4>Forums</h4>
  <% @forums.each do |forum| %>
    <h4><a href="<%= forum_path(forum) %>"><%= forum.title %></a></h4>
    <table>
      <% forum.topics.each do |topic| %>
        <tr>
          <td><a href="<%= topic_path(topic) %>"><%= topic.title %></a></td>
          <td><%= topic.user %></td>
        </tr>
      <% end %>
    </table>
  <% end %>
</div>